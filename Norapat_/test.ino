void st()
{
     /*       

      servo_down_open(); delay(200); 
      servo_down_close(); delay(200); 

    do{Motor(30,30);}while( mcp_f(1) > md_mcp_f(1) ); 
    Motor(-30,-30); delay(10);
    Motor(0,0); delay(10); 

    fline(30,30,0.3,0,'c','s',50, "a4", 20);

    mission_L(); delay(400);

    fline(20,20,0.1,0,'f','s',20, "a4", 3);

    fw(40, 40, 0.8, 0, 0, 'p', 10);
    fw(40, 40, 0.8, 0, 7, 'p', 10);


    fline_L(30, 30, 0.3, 200, 20);    // วี่งใช้เซ่นเซ่อ A1 A2 วิ่งตามเซ่น
    fline_R(30, 30, 0.3, 200, 20);    // วี่งใช้เซ่นเซ่อ A5 A6 วิ่งตามเซ่น

    mission(20, 20, 0.3, 90, "big"); delay(400);

    servo_down_open();
    delay(100);
    Motor(-20,-20);
    delay(200);
    fline(60,60,0.3,200,'n','s',70, "a5", 13);
 */
 
}
